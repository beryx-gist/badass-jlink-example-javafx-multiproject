plugins {
    id "org.javamodularity.moduleplugin" version "1.6.0"
}

version = "1.0.0"

subprojects {
    apply plugin: 'java'
    apply plugin: 'org.javamodularity.moduleplugin'

    repositories {
        jcenter()
    }
}
